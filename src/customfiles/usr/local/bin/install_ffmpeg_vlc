#!/bin/sh

DESTDIR=/
DISTDIR=/dist/pack

echo "Installing ffmpeg and vlc libraries..."; \
echo "...";  

 tar -xpzf ${DISTDIR}/ffmpeg_vlc.tar.xz -C ${DESTDIR}

 tar -xpzf ${DISTDIR}/dbusconf.tar.xz -C ${DESTDIR}

echo "All done."





